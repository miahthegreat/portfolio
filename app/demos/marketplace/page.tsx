export default function MarketplaceDemoPage() {
  return (
    <div className="mx-auto max-w-6xl px-4 py-12 sm:px-6">
      <h1 className="text-2xl font-semibold text-foreground">Services Marketplace (demo)</h1>
      <p className="mt-2 text-muted-foreground">
        Search, cart, and order history. DB + API + UI coming in Step 6.
      </p>
    </div>
  );
}
