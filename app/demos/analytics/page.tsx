export default function AnalyticsDemoPage() {
  return (
    <div className="mx-auto max-w-6xl px-4 py-12 sm:px-6">
      <h1 className="text-2xl font-semibold text-foreground">Analytics & Forecasting (demo)</h1>
      <p className="mt-2 text-muted-foreground">
        Metrics, charts, and forecast sandbox. DB + API + UI + tests coming in Step 7.
      </p>
    </div>
  );
}
